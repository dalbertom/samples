group='org.example'
version='1.0'

subprojects {
  apply plugin: 'java'
  apply plugin: 'application'
}

project(':A') {
  run {
    mainClassName = 'MainA'
  }
}

project(':B') {
  run {
    mainClassName = 'MainB'
  }
}
