group='org.example'
version='1.0'

subprojects {
  apply plugin: 'java'
  apply plugin: 'application'
}

project('Y') {
  dependencies {
    //compile project(':A')
  }

  run {
    mainClassName = 'MainY'
  }
}

project('Z') {
}
